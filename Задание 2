# Введите четырёхзначное число
number = input("Введите четырёхзначное число: ")
# Проверьте, что введенная строка содержит ровно четыре символа
if len(number) == 4 and number.isdigit():
# Преобразуйте строку в целое число и переверните его
reversed_number = int(number[::-1])
 # Выведите результат
    print(f"Число с обратным порядком цифр: {reversed_number}")
else:
    print("Пожалуйста, введите четырёхзначное число.")
